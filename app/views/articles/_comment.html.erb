<div>
  <h4>Comment by <%= comment.author_name %></h4>
  <p>Posted within <%= distance_of_time_in_words(comment.created_at, comment.article.created_at) %>... <%= distance_of_time_in_words(comment.created_at, Time.now) %> ago</p>
  <p class='comment'>&nbsp;<%= comment.body %></p>
</div>
